from setuptools import setup, find_packages

setup(
    name="pyrevolut",
    packages=find_packages(),
    version='0.1',
    description='A lightweight Python library to interact with the Revolut Merchant API.',
    author='Progme'
)